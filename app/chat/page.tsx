import { ChatScreen } from "@/src/components/shopping-workspace";

export const metadata = { title: "Ask Pilot | Cart Pilot" };

export default function ChatPage() {
  return <main><ChatScreen /></main>;
}
